```bash
  mkdir backend
  cd backend
  yarn init -y
  yarn add express
  yarn add @types/express
  yarn add typescript
  yarn tsc --init
  es2017
  yarn add typeorm sqlite3
  yarn multer yup cors
```
